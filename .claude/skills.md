# Skills

This directory is a symlink to `../.github/skills/` created by the bootstrap script.
Run `bash scripts/bootstrap.sh` (or `scripts\bootstrap.ps1` on Windows) to create it.

Skills are invoked with `/skill-name` in Copilot Chat or Claude Code.
